checkov_scan:
  stage: security
  image:
    name: bridgecrew/checkov:3.2.495
    entrypoint: [""]
  script:
    - checkov -d .
      --framework terraform,kubernetes,helm
      --skip-path gitops/flux/clusters/prod/flux-system
      -o junitxml | tee checkov_reports.xml
  artifacts:
    when: always
    paths:
      - checkov_reports.xml
    reports:
      junit: checkov_reports.xml
